# RAG Overview

Retrieval-Augmented Generation (RAG) combines two stages:
1. Vector retrieval fetches the most relevant context.
2. The LLM uses that context to produce answers with higher factual accuracy and control.
